# reference/ — the reference game (Anime Card Farm) materials

- `anime-card-farm-teardown.html` — the full ACF teardown (images embedded in the HTML).
- `acf-unpacked/` — **canonical readable set**: the teardown as markdown (879 lines) + the 53 frames with descriptive filenames (task 2026-08-09-001, build chat). Read this first.
- `images/` (53 JPEGs) + `images/index.json` (image → section/caption) — hub extraction 2026-08-10 with the caption index. The `FRAME-NN` numbering cited in `notes/transcript-frame-reconciliation-2026-08-10.md` §4 and flow spec v1.1 maps 1:1 to the `acf-unpacked` numbering (frame-05 = 005-…).
- **The Gemini video reconstructions live in `evidence/`** (video-01…06) — do NOT duplicate them here. `evidence/README.md` maps what each proves.

One source of truth: transcripts in `evidence/`, readable teardown + frames in `reference/acf-unpacked/`, captioned frame index in `reference/images/`.
